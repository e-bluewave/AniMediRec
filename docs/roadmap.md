# ロードマップ

このファイルは **upsert 専用**。フェーズの進捗が変わったら書き換える
（履歴を積まない。今の進捗状態だけを反映する）。

## Phase 1 — サーバー不要の改善（完了）

⑥日付順ソート／②個体管理番号／⑦体重単位(kg/g)／③自動保存／⑧CSV出力

→ Phase 2（クラウド化）に統合する形でまとめて実装済み。個別の「サーバー無し版」
としては提供していない（`index.html`は当初のプロトタイプのまま凍結）。

## Phase 2 — クラウド化（Firebase無料枠）（主要部分は完了）

①施設ログイン／④多端末同期・オフライン対応／⑤論理削除・ゴミ箱

- 完了: 認証・施設スコープ・役割(admin/staff/viewer)、Firestoreリアルタイム同期、
  オフライン永続化、個体の論理削除＋ゴミ箱、CSV/JSON出力、印刷用カルテ（簡易版）。
- 未着手: 監査ログ画面、記録・体重個別のゴミ箱UI、施設設定画面。
  詳細は `docs/backlog.md`。

## Phase 3 — 拡張（未着手）

- 本格的なPDFカルテ（体重グラフ埋め込み・通院用サマリーモード）
- 権限の細分化（現状はadmin/staff/viewerの3段階のみ）
- 投薬リマインダー・体重急変の自動警告
- マイクロチップ番号等の追加識別情報

## 継続対応（機能フェーズではないが常時ウォッチ）

- MANDATORYルール9対応: ネイティブダイアログ(`confirm()`/`prompt()`)を
  自前モーダルへ置き換える（docs/backlog.md「既知の負債」参照）
